{
  "private": true,
  "displayName": "Homebridge Deconz unreachable light",
  "name": "homebridge-deconz-unreachable-light",
  "version": "1.0.0",
  "description": "Set switch based on deconz light if reachable or not",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/ChristianKlee/homebridge-deconz-unreachable-light.git"
  },
  "bugs": {
    "url": "https://github.com/ChristianKlee/homebridge-deconz-unreachable-light/issues"
  },
  "engines": {
    "node": ">=10.17.0",
    "homebridge": ">=1.3.0"
  },
  "main": "dist/accessory.js",
  "scripts": {
    "watch": "npm run build && npm link && nodemon",
    "prepublishOnly": "npm run lint && npm run build"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.31",
    "homebridge": "^1.3.1",
    "nodemon": "^2.0.7"
  }
}
